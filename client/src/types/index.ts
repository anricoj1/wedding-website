import { ExtendsProps, ContainerProps, FlexboxProps, GalleryProps, CountdownWidgetProps } from 'types/baseprops';
import { RouteProps, PageProps, NavProps } from 'types/routeprops';
import { ModalProps, UseModalProps, RenderModalProps } from 'types/modalprops';


export type {
    ExtendsProps,
    ContainerProps,
    FlexboxProps,
    RouteProps,
    GalleryProps,
    PageProps,
    NavProps,
    ModalProps,
    RenderModalProps,
    UseModalProps,
    CountdownWidgetProps
}