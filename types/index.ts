import { LayoutProps, ContainerProps, FlexboxProps, GalleryProps, CountdownWidgetProps } from 'types/base';
import { RouteProps, NavProps } from 'types/routes';


export type {
    LayoutProps,
    ContainerProps,
    FlexboxProps,
    RouteProps,
    GalleryProps,
    NavProps,
    CountdownWidgetProps
}